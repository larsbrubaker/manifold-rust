{
  "name": "manifold-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch run dev.ts",
    "build:wasm": "powershell -File build-wasm.ps1",
    "build": "bun run build.ts"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
